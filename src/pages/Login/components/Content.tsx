import Stack from "@mui/material/Stack";

const Content = () => {
  return (
    <Stack
      sx={{
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center"
      }}
    >
      <img src="/public/login-img.svg" />
    </Stack>
  );
};

export default Content;
